<Page
    x:Class="ShaderForge.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShaderForge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dx="using:DXControls"
    mc:Ignorable="d">

    <Grid Background="#1d1d1d">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>            
        </Grid.ColumnDefinitions>
        
        <ScrollViewer x:Name="scroll">
            <Canvas x:Name="canvas"/>
        </ScrollViewer>

        <Button Width="40" Height="40" VerticalAlignment="Top" Margin="20" Click="OnAdd"> 
            <FontIcon Glyph="&#xE109;" FontSize="16" Foreground="White"/>
        </Button>

        <Border Grid.Column="1" Background="#0d0d0d">
            <StackPanel>
                <dx:DXSwapShainPanel x:Name="dx" Height="250" Width="350"/>
                <Button Content="Compile" Click="OnCompile"/>
            </StackPanel>
        </Border>
    </Grid> 
</Page>
